(()=>{const storageKey='shopify_last_collection';const formatMoney=(cents,format)=>{const value=(Number(cents||0)/100).toFixed(2);const [whole,decimal]=value.split('.');const amount=Number(whole).toLocaleString('en-IN');return(format||'{{ amount }}').replace(/\{\{\s*amount_no_decimals\s*\}\}/,amount).replace(/\{\{\s*amount\s*\}\}/,`${amount}.${decimal}`).replace(/\{\{\s*amount_with_comma_separator\s*\}\}/,`${amount.replace(/,/g,'.')},${decimal}`)};const initBreadcrumb=(root)=>{const link=root.querySelector('[data-product-collection-link]');if(!link)return;try{const stored=JSON.parse(localStorage.getItem(storageKey));if(stored?.title&&stored?.url&&typeof stored.url==='string'&&stored.url.startsWith('/collections/')){link.textContent=stored.title;link.href=stored.url}}catch(error){/* Ignore unavailable or malformed storage. */}};const setPanel=(trigger,panel,open,animate=true)=>{trigger.setAttribute('aria-expanded',String(open));if(!animate){panel.style.height=open?'auto':'0px';return}if(open){panel.style.height=`${panel.scrollHeight}px`;panel.addEventListener('transitionend',()=>{if(trigger.getAttribute('aria-expanded')==='true')panel.style.height='auto'},{once:true})}else{panel.style.height=`${panel.scrollHeight}px`;requestAnimationFrame(()=>{panel.style.height='0px'})}};const init=(root)=>{if(!root||root.dataset.customProductReady==='true')return;root.dataset.customProductReady='true';initBreadcrumb(root);root.querySelectorAll('[data-product-collapsible]').forEach(item=>{const trigger=item.querySelector('[data-collapsible-trigger]');const panel=item.querySelector('[data-collapsible-panel]');if(!trigger||!panel)return;setPanel(trigger,panel,trigger.getAttribute('aria-expanded')==='true',false);trigger.addEventListener('click',()=>setPanel(trigger,panel,trigger.getAttribute('aria-expanded')!=='true'))});if(typeof subscribe==='function'&&typeof PUB_SUB_EVENTS!=='undefined'){root._variantUnsubscribe=subscribe(PUB_SUB_EVENTS.variantChange,({data})=>{if(String(data.sectionId)!==String(root.dataset.section))return;const price=root.querySelector('[data-atc-price]');if(price){price.textContent=data.variant?.available?`· ${formatMoney(data.variant.price,root.dataset.moneyFormat)}`:'';price.hidden=!data.variant?.available}})}};const initAll=(scope=document)=>scope.querySelectorAll('product-info.main-product').forEach(init);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>initAll());else initAll();document.addEventListener('shopify:section:load',event=>initAll(event.target))})();
