(()=>{const save=(section)=>{if(!section)return;const {collectionHandle:handle,collectionTitle:title,collectionUrl:url}=section.dataset;if(!handle||!title||!url)return;try{localStorage.setItem('shopify_last_collection',JSON.stringify({handle,title,url}))}catch(error){/* Storage can be unavailable in privacy modes. */}};const init=(scope=document)=>scope.querySelectorAll('[data-collection-context]').forEach(save);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>init());else init();document.addEventListener('shopify:section:load',event=>init(event.target))})();
